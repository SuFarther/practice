[算数运算符]
    + - * / %
[赋值运算符]
    = -= += *= /+ %= .=

[自增自减运算符]
   ++ --

[比较运算符]
   > < >= <= == === != !==
   ==
     等于  只判断值而不比较数据的类型
   ===
      不仅仅比较值还要比较类型

[逻辑运算符]
这个你必要要会
    && ｜｜ ！

    && 逻辑与 并且 有一个为假就是假 多个为假的还为假 只有同时为真的时候才为真

    ｜| 逻辑或 或者   只要有一个为真就是真 多个同时为假才为假

    ！ 就是假变成真的 真的变成假的

    你不会没事无所谓好不好